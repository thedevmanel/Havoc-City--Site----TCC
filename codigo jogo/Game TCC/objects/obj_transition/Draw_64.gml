/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

// Desenhar o objeto de transição
draw_self();

// Configurar a cor de fundo para o efeito de transição
draw_set_color(c_black); // Pode ajustar a cor conforme necessário
draw_set_alpha(alpha);
draw_rectangle(0, 0, display_get_width(), display_get_height(), false);
draw_set_alpha(1);





